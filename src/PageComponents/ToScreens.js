import React from "react";
import "../PageComponents/toScreens.css"
const ToScreens = (props) => {
  return (
    <div className="to-screens">
          <h5>{props.screenName}</h5>
    </div>
  );
};

export default ToScreens;
